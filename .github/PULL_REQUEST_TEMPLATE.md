## What is this PR?

<!-- New tool/stack/model entry, correction, code change? -->

## Checklist

- [ ] Verdict reflects real, hands-on use (not marketing pages)
- [ ] At least one **receipt** linked (project / screenshot / benchmark)
- [ ] `limitations` lists named, specific downsides
- [ ] Affiliate links are declared (`affiliate: declared` if any tracking params)
- [ ] `last_verified` is today (YYYY-MM-DD)
- [ ] `npm run validate` passes locally
- [ ] For UI changes: read DESIGN.md; `npm run check && npm run build && npm test && npm run test:dist` pass
