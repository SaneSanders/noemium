---
interface Props {
  title: string;
  sub?: string;
}

const { title, sub } = Astro.props;
---

<header class="cover-lede">
  <h1>
    <slot>{title}</slot>
  </h1>
  {sub && (
    <p>
      {sub}
      <slot name="sub" />
    </p>
  )}
</header>
